name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install dfx
      run: DFXVM_INIT_YES sh -ci "$(curl -fsSL https://internetcomputer.org/install.sh)"
    - name: Install cargo-make
      run: cargo install --force cargo-make
    - name: Build and test
      run: cargo make
