# Development overrides - adds Cloudflare Tunnel
services:
  website:
    container_name: decent-cloud-website-dev
    ports:
      - "59000:3000"  # Dev: host 59000 -> container 3000
    networks:
      - decent-cloud-dev

  api:
    container_name: decent-cloud-api-dev
    ports:
      - "59001:8080"  # Dev: host 59001 -> container 8080
    # Unset LEDGER_DIR to allow fallback to temp directory
    # This enables multiple dev containers and easier cleanup
    environment:
      LEDGER_DIR: /tmp/ledger-dev  # Use temp directory for dev
    networks:
      - decent-cloud-dev

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: decent-cloud-tunnel-dev
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN
    networks:
      - decent-cloud-dev
    depends_on:
      website:
        condition: service_healthy
      api:
        condition: service_healthy

networks:
  decent-cloud-dev:
    driver: bridge
